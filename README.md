# audio-data-pipeline
A Python-based data pipeline for normalizing complex audio metadata. Features custom fuzzy-matching algorithms and hierarchical taxonomy extraction to reconcile internal libraries with external datasets (Spotify/Essential Mix)
